# دليل المساهمة - Contributing Guide

## مرحباً بك في مشروع Delivery Management App!

نحن نرحب بالمساهمات من الجميع. هذا الدليل يوضح كيفية المساهمة في المشروع.

## 📋 جدول المحتويات

1. [كيفية البدء](#كيفية-البدء)
2. [معايير الكود](#معايير-الكود)
3. [عملية المساهمة](#عملية-المساهمة)
4. [الإبلاغ عن المشاكل](#الإبلاغ-عن-المشاكل)
5. [طلب ميزات جديدة](#طلب-ميزات-جديدة)

## كيفية البدء

### 1. Fork المشروع
انقر على زر "Fork" في أعلى الصفحة

### 2. استنساخ المشروع
```bash
git clone https://github.com/YOUR-USERNAME/delivery-management-app.git
cd delivery-management-app
```

### 3. إنشاء فرع جديد
```bash
git checkout -b feature/your-feature-name
# أو
git checkout -b fix/your-bug-fix
```

### 4. الإعداد
```bash
flutter pub get
flutter pub run build_runner build
```

## معايير الكود

### هيكلة الكود

#### 1. تنظيم الملفات
```
lib/
├── config/         # الإعدادات والثوابت
├── models/         # نماذج البيانات
├── providers/      # إدارة الحالة
├── screens/        # الشاشات
├── widgets/        # المكونات المشتركة
├── services/       # الخدمات
└── utils/          # الأدوات المساعدة
```

#### 2. التسمية
- **ملفات**: `snake_case.dart`
- **Classes**: `PascalCase`
- **Functions & Variables**: `camelCase`
- **Constants**: `UPPER_SNAKE_CASE`

```dart
// مثال
class DeliveryProvider extends ChangeNotifier {
  final String userId;
  List<Delivery> _deliveries = [];
  
  static const String DEFAULT_TYPE = 'jawy';
  
  void loadDeliveries() {
    // ...
  }
}
```

#### 3. التعليقات
```dart
// تعليق سطر واحد للتوضيح البسيط

/// توثيق للدالة أو الكلاس
/// يشرح الغرض والاستخدام
void someFunction() {
  // ...
}

// TODO: وصف المهمة المطلوبة
// FIXME: وصف المشكلة المراد حلها
```

### معايير Flutter

#### 1. Widgets
```dart
// استخدم const عندما يكون ممكناً
const Text('مرحباً');

// فصل الـ widgets الكبيرة إلى widgets أصغر
class MyWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        _buildHeader(),
        _buildContent(),
        _buildFooter(),
      ],
    );
  }
  
  Widget _buildHeader() => Text('Header');
  Widget _buildContent() => Text('Content');
  Widget _buildFooter() => Text('Footer');
}
```

#### 2. State Management
```dart
// استخدم Provider للحالة العامة
Consumer<DeliveryProvider>(
  builder: (context, deliveryProvider, child) {
    return Text('${deliveryProvider.deliveries.length}');
  },
)

// استخدم setState للحالة المحلية
setState(() {
  _counter++;
});
```

#### 3. Async/Await
```dart
// استخدم async/await بدلاً من then
Future<void> loadData() async {
  try {
    final data = await fetchData();
    // معالجة البيانات
  } catch (e) {
    // معالجة الخطأ
  }
}
```

### التصميم

#### 1. الألوان
```dart
// استخدم الألوان من Theme
Theme.of(context).colorScheme.primary
Theme.of(context).colorScheme.secondary

// لا تستخدم ألوان ثابتة
// ❌ Container(color: Color(0xFF7B2CBF))
// ✅ Container(color: Theme.of(context).colorScheme.primary)
```

#### 2. المسافات
```dart
// استخدم SizedBox للمسافات
const SizedBox(height: 16)
const SizedBox(width: 8)

// استخدم EdgeInsets للحشو
const EdgeInsets.all(16)
const EdgeInsets.symmetric(horizontal: 16, vertical: 8)
```

#### 3. RTL Support
```dart
// استخدم Directionality عند الحاجة
Directionality(
  textDirection: TextDirection.rtl,
  child: child,
)
```

### معالجة الأخطاء

```dart
try {
  await someOperation();
} catch (e) {
  // سجل الخطأ
  print('Error: $e');
  
  // أظهر رسالة للمستخدم
  Helpers.showSnackBar(
    context,
    'حدث خطأ أثناء العملية',
    isError: true,
  );
}
```

## عملية المساهمة

### 1. اكتب الكود

اتبع المعايير أعلاه وتأكد من:
- [ ] الكود يعمل بشكل صحيح
- [ ] لا توجد تحذيرات من المحلل
- [ ] الكود منسق بشكل صحيح

### 2. اختبر التغييرات

```bash
# تشغيل التطبيق
flutter run

# فحص الكود
flutter analyze

# تنسيق الكود
flutter format .
```

### 3. Commit التغييرات

```bash
git add .
git commit -m "نوع: وصف موجز للتغيير"
```

**أنواع الـ Commits:**
- `feat`: ميزة جديدة
- `fix`: إصلاح مشكلة
- `docs`: تحديث التوثيق
- `style`: تغييرات في التنسيق
- `refactor`: إعادة هيكلة الكود
- `test`: إضافة اختبارات
- `chore`: مهام صيانة

**أمثلة:**
```bash
git commit -m "feat: إضافة شاشة المخزون"
git commit -m "fix: إصلاح حساب العمولات"
git commit -m "docs: تحديث README"
```

### 4. Push إلى GitHub

```bash
git push origin feature/your-feature-name
```

### 5. إنشاء Pull Request

1. اذهب إلى صفحة المشروع على GitHub
2. انقر على "New Pull Request"
3. اختر الفرع الخاص بك
4. أضف عنوان ووصف واضح
5. انقر على "Create Pull Request"

#### قالب Pull Request:

```markdown
## الوصف
وصف موجز للتغييرات التي تم إجراؤها

## نوع التغيير
- [ ] ميزة جديدة
- [ ] إصلاح مشكلة
- [ ] تحسين
- [ ] توثيق

## الاختبار
كيف تم اختبار التغييرات؟

## Screenshots (إذا كان مناسباً)
أضف صور للتغييرات في الواجهة

## Checklist
- [ ] الكود يتبع معايير المشروع
- [ ] تم اختبار التغييرات
- [ ] التوثيق محدث (إذا لزم الأمر)
- [ ] لا توجد تحذيرات جديدة
```

## الإبلاغ عن المشاكل

### قبل الإبلاغ
- [ ] ابحث في Issues الموجودة
- [ ] تأكد من المشكلة قابلة للتكرار
- [ ] جمع معلومات عن البيئة

### قالب Issue:

```markdown
## وصف المشكلة
وصف واضح وموجز للمشكلة

## خطوات إعادة الإنتاج
1. اذهب إلى '...'
2. اضغط على '...'
3. شاهد الخطأ

## السلوك المتوقع
ماذا كنت تتوقع أن يحدث؟

## السلوك الفعلي
ماذا حدث فعلياً؟

## Screenshots
إذا كان مناسباً، أضف صور

## البيئة
- الجهاز: [مثال: Samsung Galaxy S21]
- نظام التشغيل: [مثال: Android 11]
- إصدار التطبيق: [مثال: 1.0.0]
- إصدار Flutter: [مثال: 3.10.0]

## معلومات إضافية
أي سياق آخر حول المشكلة
```

## طلب ميزات جديدة

### قالب Feature Request:

```markdown
## المشكلة
وصف واضح للمشكلة التي تحلها هذه الميزة

## الحل المقترح
وصف واضح لما تريد أن يحدث

## البدائل
وصف أي حلول بديلة فكرت فيها

## معلومات إضافية
أي سياق أو صور إضافية
```

## قواعد السلوك

### كن محترماً
- استخدم لغة ودودة وشاملة
- احترم وجهات نظر الآخرين
- اقبل النقد البناء بلطف

### كن مساعداً
- ساعد المبتدئين
- شارك معرفتك
- كن صبوراً

### كن محترفاً
- ركز على المشكلة وليس الشخص
- تجنب الهجمات الشخصية
- احترم قرارات maintainers

## أسئلة؟

إذا كان لديك أي أسئلة:
- افتح Discussion في GitHub
- راجع الوثائق الموجودة
- ابحث في Issues السابقة

## الترخيص

بمساهمتك، فإنك توافق على أن تكون مساهماتك مرخصة تحت نفس ترخيص المشروع (MIT License).

---

شكراً لمساهمتك! 🎉

تم التحديث: ديسمبر 2024
